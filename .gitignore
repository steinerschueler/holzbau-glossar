.venv/
site/
__pycache__/
*.pyc
.DS_Store

# Pilot-Sichtcheck-Inhalt (manuell aus zimmermann_app kopiert, gehört
# nicht ins Repo — wird in W3 durch Sync-Pipeline ersetzt).
src/pilot/

# content/ wird via ./sync.sh aus dem privaten zimmermann_app befüllt
# und bewusst mit jeder inhaltlichen Welle gepusht. Nicht löschen,
# nicht im Site-Repo manuell editieren — der Sync ist unidirektional.
